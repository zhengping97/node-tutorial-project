// started operating system process
console.log('first')
setTimeout(() => {
    console.log('second')
}, 0)
console.log('third')
// completed and exited operating system process
// setTimeout is synchronous thus it get offloaded, after the immediate code (asynchronous), only then
// it invoke that callback